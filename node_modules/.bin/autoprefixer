#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../autoprefixer 2/bin/autoprefixer" "$@"
  ret=$?
else 
  node  "$basedir/../autoprefixer 2/bin/autoprefixer" "$@"
  ret=$?
fi
exit $ret
